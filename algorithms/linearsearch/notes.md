## Notes on Linear Search algorithm

```
linearSearch(A, v)
	for i < 1 to length[A]
		do if A[i] = v
			then return i
	return NULL		
```

Average performance: O(n)

Worst-case performance: O(n)

Best-case performance: O(1)
